<%= form_for @bill do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :due_at %>
  <%= f.datetime_local_field :due_at %>

  <%= f.label :amount %>
  <%= f.number_field :amount %>

  <%= f.label :is_paid %>
  <%= f.check_box :is_paid %>

  <%= f.submit %>
<% end %>